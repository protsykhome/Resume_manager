{% extends 'base.html.twig' %}

{% block title %}Edit Resume Feedback{% endblock %}

{% block body %}
    <h1>Edit Resume Feedback</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('resume_feedback_list') }}">Back to list</a>
{% endblock %}
